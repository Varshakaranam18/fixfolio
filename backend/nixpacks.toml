[phases.setup]
nixPkgs = ["jdk17", "maven"]

[phases.install]
cmds = ["mvn clean install -DskipTests"]

[phases.build]
cmds = ["mvn package -DskipTests"]

[start]
cmd = "java -jar target/fixfolio-backend-0.0.1-SNAPSHOT.jar" 